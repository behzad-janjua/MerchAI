{
  "name": "merchai",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "setup": "yarn bundle:install && yarn install --frozen-lockfile && yarn db:migrate && yarn db:seed",
    "bundle:install": "scripts/with-ruby.sh bundle config set path vendor/bundle && scripts/with-ruby.sh bundle install",
    "dev": "node scripts/run-services.mjs dev:rails dev:frontend",
    "dev:rails": "scripts/with-ruby.sh bundle exec rails server -p 4000",
    "dev:frontend": "yarn workspace @merchai/frontend dev",
    "build": "yarn workspace @merchai/frontend build",
    "test:rails": "scripts/with-ruby.sh bundle exec rspec",
    "db:migrate": "scripts/with-ruby.sh bundle exec rails db:migrate",
    "db:seed": "scripts/with-ruby.sh bundle exec rails db:seed",
    "db:create": "scripts/with-ruby.sh bundle exec rails db:create",
    "db:setup": "scripts/with-ruby.sh bundle exec rails db:create db:migrate db:seed"
  },
  "engines": {
    "node": ">=20"
  }
}
