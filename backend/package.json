{
  "name": "@merchai/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx \"src/**/*.test.ts\"",
    "start": "node dist/server.js",
    "prisma:generate": "cd .. && prisma generate --schema backend/prisma/schema.prisma",
    "prisma:migrate": "cd .. && prisma migrate dev --schema backend/prisma/schema.prisma",
    "prisma:studio": "cd .. && prisma studio --schema backend/prisma/schema.prisma",
    "prisma:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.2",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
